{
  "name": "@shipengine/connect-mock-server",
  "version": "0.0.1",
  "description": "Mock server for all ShipEngine Connect sample applications.",
  "author": "Anthony Shull <anthony.shull@auctane.com>",
  "license": "Apache-2.0",
  "main": "dist/app.js",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist/*",
    "dev": "nodemon --watch src -e ts --exec npm run start",
    "prebuild": "eslint . --ext .ts --fix",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "faker": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/faker": "^5.1.5",
    "@typescript-eslint/eslint-plugin": "^4.12.0",
    "@typescript-eslint/parser": "^4.12.0",
    "eslint": "^7.17.0",
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}
